class UserEntity {
  final String name, profilePicture, email, id;

  const UserEntity({
    required this.id,
    required this.name,
    required this.profilePicture,
    required this.email,
  });
}
